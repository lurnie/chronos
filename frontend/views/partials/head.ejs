<head>
    <meta charset='utf-8'>
    <title><%=title%> | Chronos</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel='stylesheet' href='/styles/main.css'>

    <% if (user.userId === undefined) { %>
        <script>var user = {userId: undefined, username: undefined, admin: false}</script>
    <% } else { %>
        <script>var user = JSON.parse('<%-JSON.stringify(user)%>')</script>
    <% } %>
</head>